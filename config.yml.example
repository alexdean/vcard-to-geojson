# a local postgres database for this project. will be created if it doesnt exist
# use a fresh database, not one you already have, as the process may delete data
db_name: vcard_to_geojson

# generate a key at https://console.developers.google.com
# and grant it access to the Google Maps Geocoding API
#   https://console.developers.google.com/apis/api/geocoding_backend/overview
google_api_key: "AIz..................................3pI"
